apiVersion: v1
kind: Service
metadata:
  name: httpsrv
  namespace: prod
spec:
  type: ClusterIP
  ports:
    - port: 80
      protocol: TCP
      name: http
      targetPort: 80
  selector:
    app: httpsrv
